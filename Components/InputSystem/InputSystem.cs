using OpenTK.Windowing.GraphicsLibraryFramework;

namespace FLKEngine.Components
{
    /*public static class InputSystem
    {
        static KeyboardState stateKey;

        public static bool GetKeyDown(Keys Code)
        {
            if (stateKey.IsKeyPressed(Code))
            {
                return true;
            }

            return false;
        }

        public static bool GetKey (Keys Code)
        {
            if (stateKey.IsKeyDown(Code))
            {
                return true;
            }

            return false;
        }

        public static bool GetKeyUp (Keys Code)
        {
            if (stateKey.IsKeyReleased(Code))
            {
                return true;
            }

            return false;
        }
    }*/
}